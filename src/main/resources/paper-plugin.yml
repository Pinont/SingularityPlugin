name: SingularityPlugin
version: '1.0-SNAPSHOT'
main: com.example.SingularityPlugin
api-version: '1.21'
prefix: Singularity
load: STARTUP
description: A plugin template for MC Plugin development with SingularityLib
dependencies:
  server:
    SingularityLib:
      load: BEFORE
      required: true
      join-classpath: true
